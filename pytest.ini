[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: end-to-end tests that train a small model (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
